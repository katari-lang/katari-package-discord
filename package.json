{
  "name": "katari-package-discord",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "FFI sidecar dependencies for the Katari `discord` package (src/discord.ts).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node scripts/check-limits.mjs"
  },
  "dependencies": {
    "@katari-lang/port": "0.1.0-rc10",
    "discord.js": "^14.27.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^5.9.0"
  }
}
